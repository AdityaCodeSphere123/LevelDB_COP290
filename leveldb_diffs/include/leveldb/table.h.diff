--- leveldb_base/include/leveldb/table.h	2026-04-24 22:39:53
+++ leveldb/include/leveldb/table.h	2026-04-24 13:13:15
@@ -19,6 +19,7 @@
 class RandomAccessFile;
 struct ReadOptions;
 class TableCache;
+class RangeDeletionList;
 
 // A Table is a sorted map from strings to strings.  Tables are
 // immutable and persistent.  A Table may be safely accessed from
@@ -58,6 +59,8 @@
   // be close to the file length.
   uint64_t ApproximateOffsetOf(const Slice& key) const;
 
+  RangeDeletionList* GetRangeDeletions() const;
+
  private:
   friend class TableCache;
   struct Rep;
@@ -75,6 +78,7 @@
 
   void ReadMeta(const Footer& footer);
   void ReadFilter(const Slice& filter_handle_value);
+  void ReadRangeDeletions(const Slice& handle_value);
 
   Rep* const rep_;
 };
